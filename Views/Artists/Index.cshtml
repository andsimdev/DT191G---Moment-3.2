@model IEnumerable<DT191G___Moment_3._2.Models.Artist>

@{
    ViewData["Title"] = "Index";
}

<h1>🎤 Artister</h1>

<p>
    <a asp-action="Create">Lägg till ny artist</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ArtistName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ArtistName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ArtistId">Ändra</a> |
                <a asp-action="Details" asp-route-id="@item.ArtistId">Detaljer</a> |
                <a asp-action="Delete" asp-route-id="@item.ArtistId">Radera</a>
            </td>
        </tr>
}
    </tbody>
</table>
